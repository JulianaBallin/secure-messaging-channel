"""
connection.py
--------------

Manages the database connection and session lifecycle.  
This module defines the connection URL, initializes the database engine, and provides 
a session maker for database transactions.
"""
